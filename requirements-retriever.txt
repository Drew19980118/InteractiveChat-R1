# Non-FAISS dependencies for scripts/local_retriever.py, installed after the
# locked training environment is available. The runtime (vLLM 0.6.3) is
# incompatible with NumPy 2.x. FAISS is deliberately excluded: pip CPU/GPU
# wheels can shadow the Conda GPU binding. Install it only through
# scripts/install_gpu_faiss_cuda121.sh, as documented in README.md.
numpy==1.26.4
fastapi
uvicorn
requests
orjson
