# Reproducible runtime for IGPO evaluation on Linux x86_64 NVIDIA H100 GPUs.
# Install this file before requirements.txt; see scripts/install_h100_eval_env.sh.
--extra-index-url https://download.pytorch.org/whl/cu121

# CUDA runtime. Keep these aligned with vLLM 0.6.3.
torch==2.4.0+cu121
torchvision==0.19.0+cu121
torchaudio==2.4.0+cu121

# Distributed inference and training runtime.
vllm==0.6.3
xformers==0.0.27.post2
# Ray 2.10's CLI breaks with Click 8.2+ when deepcopying command options.
click==8.1.7
ray[default]==2.10.0
# PyTorch 2.4.0 pins Triton 3.0.0. Using 3.1.0 makes pip check fail.
triton==3.0.0
# TensorDict 0.6 requires PyTorch >=2.5. Keep the 0.5 series for Torch 2.4.
tensordict==0.5.0
torchdata==0.8.0

# The local Qwen patches support the Transformers 4.49 API.
transformers==4.49.0
tokenizers==0.21.0
accelerate==1.4.0
peft==0.14.0
datasets==3.3.2
# Transformers 4.49 requires huggingface_hub<1.0. This version exposes the
# legacy `huggingface-cli` entry point; the installer creates an `hf`
# compatibility launcher inside the Conda environment.
huggingface_hub==0.28.1
safetensors==0.5.2

# Data/config packages used by the evaluation entry point.
numpy==1.26.4
pandas==2.2.3
pyarrow==18.1.0
hydra-core==1.3.2
omegaconf==2.3.0
codetiming==1.4.0
pylatexenc==2.10
pybind11==2.13.6
cachetools==5.5.0

# Install this only after the entries above are importable. This exact Python /
# PyTorch / CUDA combination has no upstream prebuilt wheel, so the setup
# script removes it for the first stage and then builds it with Ninja.
flash-attn==2.5.8
